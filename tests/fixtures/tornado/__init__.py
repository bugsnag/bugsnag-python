# Python 2 requires _init_.py files inside a folder in order for the
# folder to be considered a package and made importable. In Python 3.3
# and above, thanks to its support of implicit namespace packages, all
# folders are packages regardless of the presence of a _init_.py file.
